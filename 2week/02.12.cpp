#include <vector>
#include <iostream>
#include <random>


void quicksort(std::vector<int>& unsorted){
    int pivot_i = std::rand()/RAND_MAX * unsorted.size();
    auto pivot = unsorted[pivot_i];


}

int main(){

}
